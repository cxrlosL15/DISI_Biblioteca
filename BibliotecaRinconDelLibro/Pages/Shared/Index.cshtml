@page
@model BibliotecaRinconDelLibro.Pages.Shared.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstadoLibro[0].DescripcionEstado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EstadoLibro) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEstado)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEstadoLibro">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEstadoLibro">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEstadoLibro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
